/**
 * Canvas Constants - A4 Print Shop
 * 
 * Fixed A4 dimensions at 96 DPI for perfect 1:1 PDF mapping.
 * This ensures exports fill A4 paper without shrinking.
 */

// A4 Width @ 96 DPI = 210mm = 8.27in × 96 = 794px
export const PAGE_WIDTH = 794;

// A4 Height @ 96 DPI = 297mm = 11.69in × 96 = 1123px
export const PAGE_HEIGHT = 1123;

// Gap between PDF pages (visual separation)
export const PDF_PAGE_GAP = 40;
